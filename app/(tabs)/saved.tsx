import { StyleSheet, Text, View } from "react-native";

const Saved = () => {
    return (
        <View>
            <Text>Saved</Text>
        </View>
    );
}

const styles = StyleSheet.create({});

export default Saved;